version: "3.8"

services:
  # ==========================================
  # 1. 应用节点服务 (VPS App)
  # ==========================================
  admin_service:
    image: wwshan/strategy-admin:latest
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres_primary:5432/${POSTGRES_DB}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - SECRET_KEY=${SECRET_KEY}
    ports:
      - "${ADMIN_PORT}:8000"
    networks:
      - strategy_overlay_net
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == app]
      restart_policy:
        condition: on-failure
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/docs || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    depends_on:
      - postgres_primary
      - redis

  bot_service:
    image: wwshan/strategy-bot:latest
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_API_URL=http://admin_service:${ADMIN_PORT}
      - REDIS_URL=redis://redis:6379/0
    networks:
      - strategy_overlay_net
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == app]
      restart_policy:
        condition: on-failure
    depends_on:
      - admin_service
      - redis

  redis:
    image: redis:7-alpine
    networks:
      - strategy_overlay_net
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == app]
      restart_policy:
        condition: on-failure
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==========================================
  # 2. 主数据库节点服务 (VPS Primary)
  # ==========================================
  postgres_primary:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      REPLICATOR_PASSWORD: ${REPLICATOR_PASSWORD}
      VPS_PRIMARY_IP: ${VPS_PRIMARY_IP}
      VPS_APP_IP: ${VPS_APP_IP}
      VPS_REPLICA_IP: ${VPS_REPLICA_IP}
      VPS_STRATEGY_IP: ${VPS_STRATEGY_IP}
    volumes:
      - postgres_primary_data:/var/lib/postgresql/data
    configs:
      - source: init_replication_script
        target: /docker-entrypoint-initdb.d/01_init_replication.sh
        mode: 0755
      - source: primary_custom_entrypoint
        target: /custom_entrypoint.sh
        mode: 0755
    entrypoint: ["bash", "/custom_entrypoint.sh"]
    ports:
      - "5432:5432"
    networks:
      - strategy_overlay_net
    command: "postgres -c wal_level=replica -c hot_standby=on -c max_wal_senders=10 -c max_replication_slots=10 -c hot_standby_feedback=on -c max_connections=50 -c log_min_duration_statement=1000 -c log_line_prefix='%t [%p] %u@%d: '"
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == primary_db]
      update_config:
        order: stop-first
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 5
        window: 120s
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
    stop_grace_period: 1m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ==========================================
  # 3. 从数据库节点服务 (VPS Replica)
  # ==========================================
  postgres_replica:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGPASSWORD: ${REPLICATOR_PASSWORD}
      PRIMARY_HOST_FALLBACK: ${VPS_PRIMARY_IP}
    volumes:
      - postgres_replica_data:/var/lib/postgresql/data
    configs:
      - source: replica_entrypoint_script
        target: /replica_entrypoint.sh
        mode: 0755
    entrypoint: ["bash", "/replica_entrypoint.sh"]
    networks:
      - strategy_overlay_net
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == replica_db]
      restart_policy:
        condition: on-failure
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    depends_on:
      - postgres_primary

  # ==========================================
  # 4. 策略节点服务 (VPS Strategy)
  # ==========================================
  strategy_engine:
    image: wwshan/strategy-engine:latest
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres_primary:5432/${POSTGRES_DB}
      - REDIS_URL=redis://redis:6379/0
      - BINANCE_API_KEY=${BINANCE_API_KEY}
      - BINANCE_SECRET_KEY=${BINANCE_SECRET_KEY}
      - PROXY_URL=${PROXY_URL}
    networks:
      - strategy_overlay_net
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.role == strategy]
      restart_policy:
        condition: on-failure
    depends_on:
      - postgres_primary
      - redis

# ==========================================
# 网络、卷与配置定义
# ==========================================
networks:
  strategy_overlay_net:
    driver: overlay
    attachable: true

volumes:
  postgres_primary_data:
  postgres_replica_data:

configs:
  init_replication_script:
    name: init_replication_script_v6
    file: ./deploy/vps_primary/init_scripts/01_init_replication.sh
  primary_custom_entrypoint:
    name: primary_custom_entrypoint_v1
    file: ./deploy/vps_primary/custom_entrypoint.sh
  replica_entrypoint_script:
    name: replica_entrypoint_script_v5
    file: ./deploy/vps_replica/entrypoint.sh
